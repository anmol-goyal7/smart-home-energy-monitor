/**
 * Shared domain model: the plain value objects and enums that flow through every layer of
 * the system (readings, devices, events, thresholds, and their supporting enums).
 *
 * <p>Types here carry no behaviour beyond data access; they are the common vocabulary the
 * server, database, rules, simulator, and client packages all speak.</p>
 *
 * <p>Syllabus mapping: Unit I — Java OOP fundamentals.</p>
 */
package com.smarthome.energy.model;
