/**
 * The <em>Model</em> layer of the dashboard MVC: the observable application state
 * (overall dashboard state and per-appliance state) that the view renders and the
 * controller mutates.
 *
 * <p>Syllabus mapping: Unit II — GUI programming with Swing/AWT (MVC, observer pattern).</p>
 */
package com.smarthome.energy.client.model;
