# Python analytics module dependencies.
# Install into a virtualenv:  python -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
#
# multiprocessing is part of the standard library, so it is not listed here.

mysql-connector-python==9.1.0   # MySQL driver used to read reading/event history
tabulate==0.9.0                 # renders the analytics report as plain-text tables
