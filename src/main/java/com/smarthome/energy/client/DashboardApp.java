package com.smarthome.energy.client;

/**
 * Entry point for the Swing dashboard application.
 *
 * <p>Bootstraps the Model-View-Controller triad: constructs the {@code DashboardModel},
 * builds the {@code DashboardView} on the Swing event dispatch thread, and wires them
 * together through the {@code DashboardController}. Also starts the {@link LiveFeedClient}
 * so live readings begin flowing into the model as soon as the window opens.</p>
 *
 * <p>Syllabus mapping: Unit II — GUI programming with Swing/AWT (application bootstrap,
 * EDT usage).</p>
 *
 * @author Team member 2 (Swing MVC client)
 */
public final class DashboardApp {
    // Placeholder — main() and MVC wiring implemented by the author.
}
